{
  "name": "newapi-group-monitor",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/worker.js --local --persist-to=.wrangler/state --port 8813",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute DB --local --persist-to=.wrangler/state --file=./migrations/0001_init.sql && wrangler d1 execute DB --local --persist-to=.wrangler/state --file=./migrations/0002_sessions.sql && wrangler d1 execute DB --local --persist-to=.wrangler/state --file=./migrations/0003_channel_visibility.sql && wrangler d1 execute DB --local --persist-to=.wrangler/state --file=./migrations/0004_security.sql",
    "db:init:remote": "wrangler d1 execute newapi-group-monitor --remote --file=./migrations/0001_init.sql && wrangler d1 execute newapi-group-monitor --remote --file=./migrations/0002_sessions.sql && wrangler d1 execute newapi-group-monitor --remote --file=./migrations/0003_channel_visibility.sql && wrangler d1 execute newapi-group-monitor --remote --file=./migrations/0004_security.sql"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
